export enum ExchangeNames {
  Error = 'error',
  Delay = 'delay',
}

export enum QueueNames {
  Error = 'error',
  Requeue = 'requeue',
  Delay = 'delay',
}

export enum Suffixes {
  Dead = '.dead',
}
